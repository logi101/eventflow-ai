# ===========================================
# EventFlow AI - Environment Variables
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env to git!

# ===========================================
# Supabase (Required)
# ===========================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# Supabase Edge Functions Secrets
# Set these in Supabase Dashboard > Settings > Edge Functions
# ===========================================

# SUPABASE_URL - Auto-set by Supabase
# SUPABASE_SERVICE_ROLE_KEY - Auto-set by Supabase

# Encryption key for API credentials (min 16 chars, recommended 32)
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=your-32-char-encryption-key-here

# CORS - Your frontend URL (no trailing slash)
ALLOWED_ORIGIN=https://your-app.vercel.app

# ===========================================
# External API Credentials
# These are stored encrypted in the database per organization
# Configure via the admin UI, not here
# ===========================================

# Green API (WhatsApp) - per organization
# GREEN_API_INSTANCE=your-instance
# GREEN_API_TOKEN=your-token

# Gemini API - per organization
# GEMINI_API_KEY=your-gemini-key

# ===========================================
# Development Only
# ===========================================
# Set to 'development' to enable debug logging
NODE_ENV=production
